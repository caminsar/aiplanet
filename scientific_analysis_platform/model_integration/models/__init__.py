"""
Concrete Model Implementations and Wrappers
-------------------------------------------

This package contains specific implementations or wrappers for scientific models
that adhere to the `ScientificModel` interface defined in `model_integration.model_interface`.

Models here might be:
- Direct Python implementations.
- Wrappers around existing model executables (e.g., command-line tools).
- Clients for models running as external services.
"""

# Example: from .maxent_model import MaxEntModelWrapper
# Example: from .hydro_models import SWATModelWrapper, VICModelWrapper

print("Model implementations/wrappers package initialized.")
